namespace CombatManager.Model
{
    /// <summary>
    /// Weapon type enum.
    /// Each type has its own skill and animation prefab.
    /// Lives in Model/ like DiceTier.cs for consistency.
    /// </summary>
    public enum WeaponType
    {
        None = 0,
        Sword = 1,
        Staff = 2,
        Spear = 3
    }
}
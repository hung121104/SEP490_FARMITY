namespace CombatManager.Model
{
    /// <summary>
    /// Dice tier enum for CombatManager MVP namespace.
    /// Mirrors global DiceTier from CombatSystem (kept for legacy reference).
    /// </summary>
    public enum DiceTier
    {
        D6 = 6,
        D8 = 8,
        D10 = 10,
        D12 = 12,
        D20 = 20
    }
}
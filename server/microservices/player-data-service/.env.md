# Environment Variables

This service uses the following environment variables:

- `MONGO_URI`: MongoDB connection string (default: mongodb://localhost:27017/game)
- `PORT`: TCP port for the microservice (default: 8878)